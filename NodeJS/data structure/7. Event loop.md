```python
let soldTickets = 0;

async getTicketFromDB() {
    # return ticket id from DB after 3s
    await new Promise(resolve => setTimeout(resolve(3), 3000));
}

async function sellTicket() {
    await new Promise(resolve => setTimeout(resolve, 0));
    soldTickets += 1;
    console.log("Ticket booked!");
}

sellTicket();
sellTicket();
```